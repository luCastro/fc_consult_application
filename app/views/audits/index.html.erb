
  <main>
    <div class="margin-cale">  
      <%= month_calendar events: @audits do |date, audits| %>
        
        <%= date.day%>

        <% audits.each do |audit| %>
          <div>
            <%= audit.activity %>
            <%=link_to audit.activity, audit_path(audit) %>
          </div>

        <% end %>
      <% end %>
    </div>    
  </main>
      

  </div>
</body>
          


